# Python 3.11+, CPU only.
#
# Install PyTorch first, from the CPU index, so pip does not pull ~2.5 GB of CUDA libraries:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# The congestion classifier runs through onnxruntime, not torch.

numpy>=1.26
opencv-python>=4.9
pyyaml>=6.0

# detection and tracking (YOLO + ByteTrack)
ultralytics>=8.3

# congestion classifier: the ONNX model exported by notebooks/02_finetune.ipynb
onnxruntime>=1.17

# parameter classifier and baselines
scikit-learn>=1.4

# charts
matplotlib>=3.8

# web app
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# tests
pytest>=8.0
httpx>=0.27
